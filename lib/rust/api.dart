/// Stable public API for Flutter consumers.
///
/// Types are re-exported from the generated bridge layer.
/// Flutter UI should depend on this module, never on `lib/src/rust/` directly.
library;

export 'service.dart';
