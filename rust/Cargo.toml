[package]
name = "rust_lib_cw_ai_conductor_2026spring"
version = "0.1.0"
edition = "2021"
rust-version = "1.95"

[lib]
crate-type = ["cdylib", "staticlib"]

[dependencies]
flutter_rust_bridge = "=2.12.0"

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(frb_expand)'] }
